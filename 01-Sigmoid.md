## 1. Sigmoid 函數與導數

![alt text](Images\01-Sigmoid-001.png)

* $\sigma(x) = \dfrac{1}{1+e^{-x}}$

* $\sigma'(x) = \sigma(x),(1-\sigma(x))$

## 2. 為什麼使用 Sigmoid

由於較早期的模型都是採用線性函數之間的排列組合，導致最終訓練完的模型輸入輸出的結果也都是線性關係，但現實環境底下的場景往往很難用單純的線性關係解釋，因此才需要在訓練時加入非線性的函數。

而選擇使用 Sigmoid 的原因有以下2點
1. 輸出範圍只有 0 ~ 1
2. 整個函數本身可微分，這表示可以使用反向傳播來調整模型的權重(參數)

## 3. Sigmoid 的使用位置
Sigmoid 在模型中使用的位置是神經元中**啟動函數**的部分

![](Images\01-Sigmoid-002.png)

## 4. Sigmoid 的問題
Sigmoid 的問題其實在第一章的右半邊就有體現，主要為以下幾點
1. Sigmoid 微分後最大也只有0.25，這點在計算反向傳播時會讓梯度隨著層數的增加越來越小，引發梯度消失，到最後導致模型最開頭的參數無法得到有效的訓練
2. 當輸入數值過大或過小時，Sigmoid 的==導數==就會很接近0，從而直接導致梯度消失
3. 而就算輸入數值落在中間區段時，Sigmoid 的輸出就會近似於一個單純的線性函數，如果整個網路的輸入普遍都落在線性區，多層堆疊下模型會近似退化成一個線性變換模型

另外 Sigmoid 的算式牽扯到指數的計算，這導致不管是向前傳播還是向後傳播都需要一定的計算時間，特別是像現在的大型語言模型有特別龐大的參數量，這導致不管是使用者輸入等待模型輸出還是模型訓練時的參數調整都要耗費一大段時間

## 5. 替代方案
目前常見的解決方案是有Tanh跟Relu函數解決，這兩個函式主要解決的方面有一些不同

### 5.1 Tanh

1. 圖形
![](Images\01-Sigmoid-003.webp)

* $\tanh(x) = \frac{e^{x} - e^{-x}}{e^{x} + e^{-x}}$
* $\tanh'(x) = 1 - \tanh^2(x)$

2. 說明
Tanh的函式基本上就類似於Sigmoid的向下平移版本，同時在輸入近似為0時導數也相當靠近1，但當輸入遠離原點時導數依然會接近0，不管輸入的正負，因此同樣會影響參數更新的效率

### 5.2 Relu

1. 圖形
![](Images\01-Sigmoid-004.webp)

* $\text{ReLU}(x) = \max(0, x)$
2. 說明
雖然失去將任意輸入轉換成0 ~ 1的能力，但換來的是極快的計算速度，同時保證了大於 0 的輸入能得到完整的梯度保留。雖然Relu只是由兩段直線組合而成，但原點的轉折使得Relu不符合純線性變換的定理，每個 ReLU 神經元因為權重 $w$、偏差 $b$ 不同（$\text{ReLU}(wx+b)$），折點會出現在不同位置。把很多個不同折點的分段直線加起來，就能逼近任意的曲線
    ```
    單一 ReLU：     ___/        （一個折點）
    多個疊加：   __/‾\_/‾\__     （很多折點 → 近似曲線）
    ```
但Relu也有實務上的問題，最重要的是輸入小於0的範圍，輸出就會是0，一旦有單一節點的梯度等於0，就會導致在計算反向傳播的梯度時前面幾個節點(由最靠近輸出端到輸入端來看)的梯度貢獻歸0
## REF
https://medium.com/@adea820616/activation-functions-sigmoid-relu-tahn-20e3ae726ae